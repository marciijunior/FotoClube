VITE_GRAPHQL_URL=http://localhost:3002/graphql

# URL base para uploads (ajuste para produção)
VITE_UPLOADS_URL=http://localhost:3002/uploads

# Copy this file to `.env` and restart the dev server when changing the value.

# Backend sensitive variables
DATABASE_URL=
JWT_SECRET=
ADMIN_PASSWORD=